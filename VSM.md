# SVM 指的是 Support Vector Machine（支援向量機）

在人工智慧（AI）與機器學習中，SVM 指的是 Support Vector Machine（支援向量機），是一種常用的**監督式學習演算法**，主要用於**分類（classification）與回歸（regression）**問題。

## SVM 的核心概念
- SVM 的主要目標是：
  👉 在資料之間找到一條「最佳分隔線」（或平面），把不同類別區分開來。這條分隔線稱為：**決策邊界（Decision Boundary）**。  而 SVM 的特色是：選擇讓兩類資料「間隔最大」的那條線。
- SVM 會：
  - 嘗試畫一條直線把兩群分開。
  - 並且讓兩邊距離最近的點「距離最大」
  - 這個距離叫做：Margin（間隔）
  - SVM = 找最大 Margin 的分類器。

## 什麼是「支援向量」？
在資料集中，並不是所有點都關鍵，只有那些「最靠近分隔線」的點，才會影響模型。這些點就叫做：**Support Vectors（支援向量）**，它們決定了分隔線的位置。












