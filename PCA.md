# PCA 
在人工智慧與機器學習中，PCA 指的是：Principal Component Analysis（主成分分析），是一種常見的**降維（dimensionality reduction）技術**，用於將高維資料簡化，同時保留最重要的資訊。

## PCA 的核心概念
- PCA 的主要目標是：
  - 把原本很多個變數，轉換成少數幾個「新變數」
  - 並且這些新變數仍然保留資料中「最多的資訊」
  - 這些新變數稱為：主成分（Principal Components）

## 為什麼需要 PCA？
- 在現實中，資料可能會有：
  - 很多欄位（高維度）
  - 有些欄位彼此高度相關
  - 計算成本高
  - 不容易視覺化
- PCA 可以幫你：
  - 降低維度（例如：100維 → 2維）
  - 去除冗餘資訊
  - 簡化模型
  - 方便畫圖觀察資料分布

## PCA 在做什麼？（直觀理解）
想像你有一堆 2D 資料點：資料其實沿著「斜線方向」分布
- PCA 會：就像「重新旋轉座標軸」
  - 找到資料變化最大的方向（第一主成分）
  - 找到第二大的方向（第二主成分）
  - 把資料投影到這些新軸上

## 主成分的特性
- 每一個主成分都有：**彼此正交（互相獨立）**
- 按重要性排序：PC1：最多變異（最重要），PC2：第二多變異....。通常只取前幾個主成分就夠了！

## 簡單例子：
- 假設你有三個變數：身高、體重、腳長...。這些其實彼此相關
- PCA 可以轉成：主成分1：體型（整體大小）、主成分2：比例（身材差異），用較少維度表達原本資料。

## PCA 的數學本質（簡述）
- PCA 其實是在做：
  - 找資料的最大變異方向
  - 計算共變異數矩陣
  - 做 特徵值分解（Eigen decomposition）
- 特徵向量 = 主成分方向
- 特徵值 = 該方向的重要程度

## PCA 的優點 / 缺點
- 優點
  - 降維有效（保留大部分資訊）
  - 減少噪音影響
  - 加快模型訓練速度
  - 視覺化高維資料（2D / 3D）
- 缺點
  - 主成分不易解釋（失去原始意義）
  - 無法處理非線性關係（線性方法）
  - 需要資料標準化（scale 很重要）

## SVM vs PCA（快速對比）
|方法  |類型  |用途  |
|--|--|--|
|SVM  |監督式學習  |分類 / 回歸  |
|PCA  |非監督式學習  |降維 / 資料壓縮  |

## 總結
- PCA 是一種「把高維資料壓縮到低維」的方法
- 核心是保留「最大變異」資訊
- 常用於資料前處理與視覺化
