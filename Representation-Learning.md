# Representation Learning
表現學習{表示學習、表徵學習}（Representation Learning），在機器學習與深度學習中，也被稱**為特徵學習（Feature Learning）**。
指的是讓模型自動從原始資料中學習出有用的特徵表示（representations），而不是完全依賴人類手工設計特徵。

## 為什麼表現學習這麼重要？
表現學習之所以成為現代 AI（特別是大語言模型、生成式 AI）的基石，主要有以下三個原因：
|優勢	|說明  |
|-----|------|
|擺脫人工限制	|讓機器自己找特徵，能發現人類專家直覺上沒想到的潛在規律。|
|降維與去噪	|原始數據（如高畫質影像）維度極高且包含許多雜訊。好的表現學習能將其壓縮成精簡、高密度的向量，只保留最重要的資訊。|
|遷移學習（Transfer Learning）	|學習到的「表現」往往具有通用性。例如，一個在百萬張通用圖片上學會「如何看懂世界」的模型，它的表現能力可以輕易遷移到醫療影像辨識上。|

## 常見的應用與例子
- 文字（Word Embeddings / Transformers）：
  像 Word2Vec 或 BERT/GPT。它們將人類的詞彙轉換成高維度的向量。在這些向量空間中，意思相近的詞（如「貓」和「狗」）距離會很近，甚至能做向量運算：
  國王 - 男人 + 女人 = 女王
  $$\text{國王} - \text{男人} + \text{女人} \approx \text{女王}$$
- 影像：
  自編碼器（Autoencoders）或對比學習（Contrastive Learning），能將圖片壓縮成特徵向量，用於圖片檢索、人臉辨識或異常檢測。









